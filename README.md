# Titanic Survival Prediction Project 🚢
This project is an end-to-end Supervised Machine Learning pipeline.

## Source Code:
```python
# -*- coding: utf-8 -*-
"""Ahmed_Matter_ML_classification_Titanic.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1kJt-94fR9rUi-zJDwDizu14J6Ti2ycHT

# **`Notebook_ML_classification_Titanic_Ahmed_Matter`**

Project Showcase: Titanic Survival Prediction: An End-to-End ML Benchmarking Pipeline

**First : Data Connection , EDA , preprocess**
"""

#Connect to gdrive

from google.colab import drive
drive.mount('/content/drive')

#import lib

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.preprocessing import StandardScaler
from sklearn.preprocessing import LabelEncoder
from sklearn.preprocessing import OneHotEncoder
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.tree import DecisionTreeClassifier
from sklearn.naive_bayes import GaussianNB
from sklearn.svm import SVC
from sklearn.ensemble import GradientBoostingClassifier
from sklearn.metrics import accuracy_score, classification_report
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score, confusion_matrix, classification_report
from google.colab import files

#read dataset
train_df = pd.read_csv('/content/drive/MyDrive/train_Taitanic.csv')
test_df = pd.read_csv('/content/drive/MyDrive/test_Taitanic.csv')

train_df.head(10)

test_df.head(10)

#shape of data
print(f'test data shape is: {test_df.shape} train data shape is {train_df.shape}')

#train data all cols info
train_df.info()

#train data all cols null
train_df.isnull().sum()

#test data all cols info
test_df.info()

#tet data all cols null
test_df.isnull().sum()

#type of data

print(f'test data shape is:\n {test_df.dtypes} \n \n \n train data shape is:\n {train_df.dtypes}')

#data cleaning

columns_to_remove_train = ['PassengerId', 'Name', 'Ticket', 'Cabin']
columns_to_remove_test = ['Name', 'Ticket', 'Cabin']

age_median = train_df['Age'].median()
fare_median = train_df['Fare'].median()

train_df = train_df.drop(columns=columns_to_remove_train, axis=1)
test_df = test_df.drop(columns=columns_to_remove_test, axis=1)


train_df['Age'] = train_df['Age'].fillna(age_median)
test_df['Age'] = test_df['Age'].fillna(age_median)

train_df['Fare'] = train_df['Fare'].fillna(fare_median)
test_df['Fare'] = test_df['Fare'].fillna(fare_median)

train_df.dropna(subset=['Embarked'], inplace=True)
test_df.dropna(subset=['Embarked'], inplace=True)

train_df.info()

test_df.info()

train_df

test_df

"""**Second : Encoding & Scaling**"""

#Encoding

#Encode Sex Col

gender_mapping = {'male': 0, 'female': 1}


train_df['Sex'] = train_df['Sex'].map(gender_mapping)
test_df['Sex'] = test_df['Sex'].map(gender_mapping)

encoder = OneHotEncoder(sparse_output=False)
#Encode embarked Col
embarked_encoded_train = encoder.fit_transform(train_df[['Embarked']])
embarked_encoded_test = encoder.transform(test_df[['Embarked']])


encoded_columns = encoder.get_feature_names_out(['Embarked'])

train_embarked_df = pd.DataFrame(embarked_encoded_train, columns=encoded_columns, index=train_df.index)
test_embarked_df = pd.DataFrame(embarked_encoded_test, columns=encoded_columns, index=test_df.index)


train_df = pd.concat([train_df.drop(columns=['Embarked']), train_embarked_df], axis=1)
test_df = pd.concat([test_df.drop(columns=['Embarked']), test_embarked_df], axis=1)

#Encode Pclass Col
pclass_encoded_train = encoder.fit_transform(train_df[['Pclass']])
pclass_encoded_test = encoder.transform(test_df[['Pclass']])

encoded_columns = encoder.get_feature_names_out(['Pclass'])

train_pclass_df = pd.DataFrame(pclass_encoded_train, columns=encoded_columns, index=train_df.index)
test_pclass_df = pd.DataFrame(pclass_encoded_test, columns=encoded_columns, index=test_df.index)

train_df = pd.concat([train_df.drop(columns=['Pclass']), train_pclass_df], axis=1)
test_df = pd.concat([test_df.drop(columns=['Pclass']), test_pclass_df], axis=1)


#Feature Engineering by adding 2cols familysize and is alone

train_df['FamilySize'] = train_df['SibSp'] + train_df['Parch'] + 1
test_df['FamilySize'] = test_df['SibSp'] + test_df['Parch'] + 1


train_df['IsAlone'] = (train_df['FamilySize'] == 1).astype(int)
test_df['IsAlone'] = (test_df['FamilySize'] == 1).astype(int)



#Drop columns SibSp ,Parch

columns_to_drop = ['SibSp', 'Parch']

train_df = train_df.drop(columns=columns_to_drop, axis=1)
test_df = test_df.drop(columns=columns_to_drop, axis=1)

#Scaling

scaler = StandardScaler()

numerical_cols = ['Age', 'Fare']

train_df[numerical_cols] = scaler.fit_transform(train_df[numerical_cols])

test_df[numerical_cols] = scaler.transform(test_df[numerical_cols])

"""**show data after encoding and scaling**"""

train_df

test_df

"""**Third : Train & Test Models**"""

y = train_df['Survived']
x = train_df.drop(columns=['Survived'])
X_train, X_val, y_train, y_val = train_test_split(x, y, test_size=0.2, random_state=42)

modelRF = RandomForestClassifier(n_estimators=400, random_state=42)

modelRF.fit(X_train, y_train)

y_predRF = modelRF.predict(X_val)
accuracyRF = accuracy_score(y_val, y_pred)

print(f"\n {accuracyRF * 100:.2f}%")

print(classification_report(y_val, y_pred))

log_model = LogisticRegression(max_iter=1000, random_state=42)


log_model.fit(X_train, y_train)

y_pred_log = log_model.predict(X_val)
accuracy_log = accuracy_score(y_val, y_pred_log)

print(f"\n {accuracy_log * 100:.2f}%")

print(classification_report(y_val, y_pred_log))

tree_model = DecisionTreeClassifier(max_depth=5, random_state=42)
tree_model.fit(X_train, y_train)

y_pred_tree = tree_model.predict(X_val)
accuracy_tree = accuracy_score(y_val, y_pred_tree)
print(f": {accuracy_tree * 100:.2f}%")
print(classification_report(y_val, y_pred_tree))

nb_model = GaussianNB()
nb_model.fit(X_train, y_train)

y_pred_nb = nb_model.predict(X_val)
accuracy_nb = accuracy_score(y_val, y_pred_nb)
print(f": {accuracy_nb * 100:.2f}%")
print(classification_report(y_val, y_pred_nb))

svm_model = SVC(kernel='rbf', random_state=42)
svm_model.fit(X_train, y_train)


y_pred_svm = svm_model.predict(X_val)
accuracy_svm = accuracy_score(y_val, y_pred_svm)
print(f": {accuracy_svm * 100:.2f}%")
print(classification_report(y_val, y_pred_svm))

gb_model = GradientBoostingClassifier(n_estimators=100, learning_rate=0.1, max_depth=3, random_state=42)
gb_model.fit(X_train, y_train)


y_pred_gb = gb_model.predict(X_val)
accuracy_gb = accuracy_score(y_val, y_pred_gb)
print(f": {accuracy_gb * 100:.2f}%")
print(classification_report(y_val, y_pred_gb))

"""**Fourth : Select The Best Model**"""

results = pd.DataFrame({
    'Model': ['Random Forest', 'Logistic Regression', 'Decision Tree', 'Naive Bayes', 'SVM', 'Gradient Boosting'],
    'Accuracy': [
        accuracyRF * 100,
        accuracy_log * 100,
        accuracy_tree * 100,
        accuracy_nb * 100,
        accuracy_svm * 100,
        accuracy_gb * 100
    ]
})

results = results.sort_values(by='Accuracy', ascending=False).reset_index(drop=True)

print(results)

"""**Fifth : Prediction**"""

Predictions = svm_model.predict(test_df[X.columns])

submission = pd.DataFrame({
    'PassengerId': test_df['PassengerId'],
    'Survived': Predictions
})


submission.to_csv('titanic_svm_predictions.csv', index=False)

submission

#Optional run to download the file
#files.download('titanic_svm_predictions.csv')

# ضع باقي كود المشروع هنا...
